---
import LeftChevronIcon from "@/contexts/_shared/client/presentation/components/icons/LeftChevronIcon.astro"

const { backHref, } = Astro.props
---

{ backHref && (
    <a href={backHref} class="p-2 rounded-full text-gray-600 hover:bg-gray-200 transition-colors focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-[#6faab5] -mt-1" aria-label="Ir atrás">
        <LeftChevronIcon />
    </a>
)
}
<div>
    <h2 class="text-2xl font-bold leading-tight text-gray-900">
        <slot />
    </h2>
    {
        Astro.slots.has("subtitle") && (
            <p class="mt-1 text-md text-gray-600">
                <slot name="subtitle" />
            </p>
        )
    }
</div>